// hostname "L4L Dev" // ""
sn_main_name "L4L Dev" // Hostname
sn_hostname_format1 "{hostname} {difficulty} {gamemode}" // "[{hostname} #{servernum}] {gamemode}"
sn_hostname_format2 "{hostname} {difficulty} {gamemode}" // "[{hostname} #{servernum}] {gamemode} - {difficulty}"
sn_hostname_format3 "{hostname}" // "[{hostname} #{servernum}]"

sv_allow_lobby_connect_only 0 // 1

echo specific.cfg